<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="TestTaskTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
    </style>
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/selected_bottom_nav_item_color</item>
    </style>
    <style name="RoundedCornerOverlay">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item> <!-- Размер закругления углов -->
    </style>
    <style name="Theme.TestTaskGoSport" parent="Base.Theme.TestTaskGoSport" />
</resources>